"""Frozen pre-0.2 bridge API promised by the legacy import paths."""
BRIDGE_LEGACY_API = {
    "AgentConfig", "AgentIncompleteError", "AgentLimitExhausted",
    "AgentRunResult", "AgentTimeout", "HarnessCapabilities", "ToolSpec",
    "backend_capabilities", "backend_name", "default_model",
    "resolve_agent_config", "retry_transient", "run_agent", "wall_seconds",
}
