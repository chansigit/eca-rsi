# Slurm warm pool：调度与执行设计

状态：设计草案，2026-09-12。本文记录新的需求边界和建议实现，未实现的内容不能作为当前功能使用。
首版的实际用法见 [SLURM_POOL.md](SLURM_POOL.md)，验证范围见 [SLURM_POOL_VALIDATION.md](SLURM_POOL_VALIDATION.md)。
第一版采用 FIFO + 资源匹配，不做短任务优先、公平配额、优先级老化、未来槽位预留或回填优化。
先保证资源核算和节点生命周期正确；调度算法在出现实际瓶颈后再优化。

用户本轮明确的边界取代 [issue #8](https://github.com/chansigit/eca-rsi/issues/8) 中自动申请、空闲缩零和强制只传文件路径的早期设想。

## 目标与职责

用户自行申请 Slurm allocation、启动或停止 worker、归还资源。计算池在这些已获配的资源内，
帮助多个独立流程尽早完成计算，尊重 CPU、内存、GPU 和机时限制。没有自动 sbatch、扩缩容或 scancel。

| 组件 | 职责 |
| --- | --- |
| Driver | 科学流程、模型调用、依赖关系、输入输出校验、断点与最终结果；独立于池运行 |
| Driver 内的计算客户端 | 提供任务规模和本地执行条件，比较本地与池的完成时间，提交或撤回任务 |
| Pool Scheduler | 所有客户端共享的接纳、在途资源占用、简单排队、worker 选择和状态管理 |
| Worker | 在自己实际获配的资源内执行计算，报告运行状态与测量值 |
| Slurm / 用户 | allocation 的申请、配额、时限、续期和释放 |

模块放在 RSI 的 `ecarsi/pool/`，定位为 **Slurm 专用计算池**。Pool Scheduler 是独立进程，
不由某一个分析 driver 拥有；driver 退出不能关闭共享池。Periscope 只读状态，不参与调度。
本地执行不要求启动 scheduler，也不应强制安装 Dask 后才能使用。

## 已有代码与需要补的部分

- `container/dask-pool.sh`：手动启动 scheduler/worker、接入共享 scheduler、查询状态。
- MSP 的 `msp/compute.py`：local、dask-local、连接已有池的 dask 后端；Harmony、图计算、DE 已接入，ZMIP 复用。
- `ecarsi/osp_worker.py`：OSP 已按计算与注释区分阶段，能验证并复用计算快照，但目前两段仍在同一个本地进程执行。
- MSP 的 `msp/resources.py`：已有 CPU affinity、cgroup 内存上限探测，可复用其逻辑；池注册时还必须核对 Slurm allocation/step。

当前没有基于预计完成时间的自动选址，也没有本文描述的统一接纳队列和剩余机时约束。
Dask 的 worker 注册、心跳、Future、资源标签、执行状态与传输继续复用。
RSI 补的是任务语义、Slurm 配额/时限和接纳策略；不重新实现通信、心跳或通用分布式执行器。

## 两层决策，统一确认

对每个已就绪任务，比较的是预计完成时间：

```
T_local = 本地等待 + 本地计算 + 结果落盘/校验
T_pool(w) = worker 可用前的等待 + 输入传输/读取 + worker 计算 + 输出传输/落盘/校验
```

只有满足资源和剩余机时约束的 worker 才参加比较。不能只按细胞数设一道固定门槛；
也不能因为队列长度短就认为池更快：一个运行中的长任务可能比十个短任务更影响等待时间。
第一版用任务规模、近期耗时、当前在途任务和传输量粗估，不建立复杂预测模型。
冷启动、节点性能或 I/O 差异若在实测中明显影响判断，再细化估计。

Driver 提供本地可用资源和估计，scheduler 使用全池状态给出接纳结果。
查询结果只是估计，不是资源承诺；真正派发时由 scheduler 原子地确认并登记资源占用，
防止多个 driver 同时把同一个空闲 worker 算作自己的容量。重复提交使用稳定任务 ID 去重。

三种显式位置策略：

- `local`：强制本地，在本地实际资源边界内运行。
- `pool`：只交给池；没有合适 worker 时显示原因并按明确的等待期限等待，允许用户随后加入 worker。
- `auto`：只有池的预计收益超过传输开销和估计误差时才选择池；否则本地执行。

`auto` 只在合法可执行的位置间选择。driver 没有足够内存、没有所需 GPU，或处于未获配的登录节点时，
池忙不意味着可以偷偷转本地重计算。轻量控制操作仍可本地完成。
同一 allocation 中本地计算与 worker 使用的 CPU/内存必须分开预算，不能重复计算容量。

第一版只在提交前选择本地或池，排队后不自动反复改位置。
用户撤回后要转本地，必须先收到撤回成功确认；已开始执行的任务不迁移。
池失联时先核对执行/产物状态，不能立即在本地再跑一份。

位置选择与算法选择分开。`auto` 不默认授权把 CPU 算法换成数值不同的 GPU 算法；
可用算法后端由任务声明，沿用科学运行身份和验证规则。

## 调度器需要知道什么

| 对象 | 必要信息 |
| --- | --- |
| 任务 | run/step/稳定任务 ID、输入与代码身份、算法后端、就绪时间、CPU/内存/GPU需求、数据位置、预计时长及不确定性 |
| 规模摘要 | 操作种类、细胞数、基因数、稀疏非零数、簇数/比较次数、关键计算参数；按任务选取已有信息 |
| Worker | 唯一启动身份、节点、Slurm job/step、实际 CPU/内存/GPU、剩余机时、软件环境身份、当前任务、心跳与退出状态 |
| 本地候选 | driver 的合法计算预算、本地在途任务、环境/后端能力、预计完成时间 |

任务描述保持小型；不为了估计耗时再扫描一遍大矩阵。复用输入元数据和运行记录。
规模摘要用于估计，不能替代恢复与结果校验需要的完整身份。

估计先用同类操作、相近规模的近期记录或明确的资源/耗时提示，没有历史时保守处理。
记录实际耗时和峰值内存即可逐步校准，不引入学习型调度器。
剩余机时判断使用保守耗时估计，并留出结果写入与退出余量。

## 排队与 worker 选择

第一版是 **FIFO + first-fit（按到达顺序，找第一个合适的空闲 worker）**。
每次任务加入、任务结束或 worker 加入/退出时，按队列顺序扫描就绪任务：

1. 检查 worker 的实际可用 CPU、内存、GPU 是否满足任务要求；同一 allocation 的容量不能重复计算。
2. 检查剩余机时是否覆盖预计计算时间和写入/退出余量，并核对环境和数据可达性。
3. 在满足条件的空闲 worker 中按固定注册顺序选第一个，登记占用后派发；多个任务竞争时先到者先分配。
4. 当前没有合适 worker 的任务保留原队列位置，并显示原因；继续看后面的任务，让其他可用资源正常工作。

CPU、内存、GPU 和剩余机时作为可行性条件，不先设计一套复杂的加权评分。
多个 worker 规格不同时，可能暂时不是最省资源的分配，这是第一版接受的取舍。
已运行任务不抢占，不规划尚未由模型决定的 lineage 或未来轮次。

多个 driver 共用一份队列和在途资源占用记录，避免重复分配。
等待任务只保存小型描述，不预留未来时段，也不提前向 worker 传大矩阵。

已知限制：持续绕过暂时放不下的大任务，可能让它等待较久。
先记录最长等待和跳过原因；确实发生长期等待或明显利用率问题时，再针对性增加排序或预留策略。
当前不实现短任务优先、按 run 公平配额、优先级老化、未来槽位预留和回填计划，也不以全局最优为验收目标。

## Dask 与 RSI 之间的边界

Dask 已有资源约束与调度策略，但资源标签是用户声明的抽象数量，不能自动代表 Slurm 的真实配额。
RSI 需要补充这些信息，并验证 CPU 线程数和原生内存分配也遵守边界。
参见 [Dask resources](https://distributed.dask.org/en/stable/resources.html)。

建议把 RSI 接纳策略与 Dask scheduler 放在同一服务进程内，以一个策略扩展维护等待描述和在途资源占用。
任务只在获准执行时进入 Dask 执行队列，带确定的 worker/资源限制；不能让另一条无约束的 Dask 队列推翻机时与资源判断。
初版不对这些已分配任务启用不受 RSI 约束的工作窃取。worker 丢失后必须重新做可行性判断。
既有直接 `MSP_COMPUTE_ENDPOINT=dask` 路径仍可保留，但只有经 RSI 接纳的任务才享有本文的调度策略。

这是需要先做技术验证的接入点：插件提供 worker 和任务事件，但不等于提供了全部自定义调度接口。
原子接纳、请求/取消和提交衔接要针对实际安装的 Dask 版本验证；不能在每个 driver 中轮询后自行指派，冒充全局调度。
若需要访问 scheduler 扩展接口，集中封装并约束支持版本，不散落到科学内核。
依据：[Dask scheduling](https://distributed.dask.org/en/stable/scheduling-policies.html)、
[plugins](https://distributed.dask.org/en/stable/plugins.html)、
[work stealing](https://distributed.dask.org/en/stable/work-stealing.html)。

## Slurm 生命周期和资源边界

用户启动 worker 时提供池地址；worker 从自己的真实 job/step 和进程环境取得信息后注册。
优先在用户已有 allocation 的明确 Slurm step 内启动，使用用户授予的 CPU/GPU/内存。
不能通过读整台机器的核数、内存或 GPU 数量，把未获配的部分登记进池。
`srun` 在已有 allocation 中启动 step 的语义以 [Slurm 文档](https://slurm.schedmd.com/srun.html) 和集群配置为准。

同一节点多个 worker 的配额必须分割；同一块 GPU 不能在多个进程中各自登记一遍完整容量。
Dask 的资源数值按 worker 进程分别计算，不能把 allocation 总内存重复填到每个 worker。
初版使用固定资源规格、每个 worker 同时一个重计算任务，固定其 BLAS/OpenMP/Numba 线程预算。
确有吞吐需求再增加更细的同进程并发，避免全局线程设置互相干扰。

Worker 状态：`joining → ready → draining → closed`；任何在线状态都可能因失联变成 `lost`。

- 加入：核对 job/step、配额、数据挂载和软件身份，再提供容量；同一节点的重启使用新启动身份。
- 正常退出：停止接新任务，处理已派发但尚未开始的任务，等待在途任务完成。
- 机时临近：按任务预计完成时间与安全余量停止接收放不下的任务；TERM 通知只是补充，不能等收到信号才处理。
- 意外退出：移除容量、清理失效占用，将受影响任务交回恢复逻辑；确定性输入/计算错误不盲目重试。
- 新 worker 加入：立即重新匹配排队任务；新资源不触发已运行任务迁移。

节点并非唯一的生命周期单位，同一节点可以属于不同 allocation。
结束时间等信息来自 Slurm，周期性刷新并缓存，避免每个任务或心跳都运行 `scontrol`。
时限缩短、用户撤销 allocation 和突然抢占仍可能中断任务，系统必须如实处理失败，不能保证任意任务都来得及排空。
Slurm 暂时查询失败时，不扩大已知配额或延长已知截止时间；无法确认安全的 worker 停止接新重任务。

空闲但仍在有效 allocation 中的 worker 可以一直 ready，不自动归还资源。
没有 worker 时 scheduler 仍可存在；显示等待资源及任务等待期限，等用户加入节点。

## 结果、失联与重试

计算池不接管科学完成状态。最终完成仍由 driver 校验输入身份、输出和细胞守恒，并写既有完成记录。

有文件副作用的执行使用独立 attempt 目录；任务 ID、scheduler 启动代次和 attempt 身份一并记录。
driver 在自己持有的输出锁下，只发布当前有效 attempt。旧 worker 的迟到结果不能覆盖新结果。
撤回 Future 不等于底层进程已经停止，Dask 重新执行也不等于副作用恰好发生一次。
允许纯数组计算安全重算；文件输出必须先隔离再验证发布。

driver 先持久化任务请求再提交。scheduler 重启后不假设 Dask 队列自动持久化：
由存活或恢复后的 driver 使用稳定 ID、已保存请求和产物记录进行对账，恢复需要继续的任务。
旧代次的 worker 结果先隔离，不因“断连”自动标成功或直接重复发布。
初版是单 scheduler；不引入高可用集群，但必须验证其重启恢复路径。

## 数据传输和 OSP

大型、已经落盘的数据优先使用共享文件路径。小数组与小型返回值直接传输。
选择依据是传输/序列化开销、I/O 与峰值内存；不强制所有任务走文件。
跨节点路径必须实际共享，不能把某节点的本地临时目录当成全池可读路径。

OSP 首个接入粒度是单样本连续的计算段：QC（含启用的 Scrublet/DecontX）→ 降维/聚类 → DE。
复用 OSP 公共函数和 RSI 已有的计算快照校验，把注释调用留在 driver；不把等待模型的整个 OSP 进程占在计算槽位里。
小样本可在合法本地预算下直接计算；大样本提交给池。保留原始 ID、输出格式及失败判定。

MSP/ZMIP 保持已有计算边界，按需增加规模/资源描述并通过可选适配器使用 RSI 调度。
科学包不增加对 RSI 的强制反向依赖；独立安装时 local 和现有 dask 方式继续可用。
适配器只在显式选用 RSI pool 时加载。实际接口在技术验证中确定，不先提取新的共享包。

## 模块与落地顺序

预期逻辑边界是 `client`（位置选择与提交）、`scheduler`（统一接纳和队列）、
`slurm`（allocation/step 信息与启动边界）、`worker`（资源与生命周期）。
这是职责划分，当前不创建空类或占位实现。新增子包时需要同时调整 RSI 当前显式的 setuptools 包清单。

1. **基本记录**：已有重计算记录规模、实际耗时和峰值内存，为资源需求和简单的 local/pool 判断提供依据。
2. **Slurm worker 生命周期**：注册与资源分割、动态加入、主动排空、机时截止、丢失和软件不匹配测试。
3. **简单调度**：实现共享 FIFO + first-fit，验证多客户端不重复占用资源、等待原因、撤回和重启对账；验证 Dask 接入点。
4. **OSP 计算段接入**：保持现有科学规则，以实际单样本/多样本验证本地和池输出、恢复及槽位释放。
5. **简单 auto 判断**：根据任务规模、近期耗时、本地资源和池忙碌情况选择执行位置；比较纯本地与池执行的实际开销。

后续只在运行记录证明存在问题时优化调度算法；不预建算法插件体系。

## 验收标准

必须覆盖：小任务本地更快；大任务池更快；忙池使 auto 选择合法本地；本地内存不足时不退回本地；
多个 driver 同时提交而容量不被重复承诺；节点中途加入、排空、SIGKILL、机时结束；
只剩短机时的 worker 不接预计超时任务；竞争同一可用资源的任务按到达顺序派发；资源不匹配时给出可读原因；
driver/scheduler 重启、取消竞态及迟到结果不会重复发布；OSP 计算完成后模型调用不占计算槽位。

真实验收使用用户已经提供的 Slurm allocation，不为测试自动申请或释放资源。
先做小规模合成任务检查，再做并发真实 OSP/MSP/ZMIP，记录：

- 每个任务的排队与运行时间、分配的 CPU/内存/GPU、派发时剩余机时。
- 峰值内存、worker 退出原因、重试次数，以及 local/pool 选择或继续等待的原因。

平均等待、最长等待和利用率从这些运行记录汇总，不先建设复杂的调度评测系统。
首版验收重点是资源和机时不被错误承诺、任务能执行和恢复、简单调度行为可解释。
任务执行成功与科学产物有效分别验收，不以“worker 在线”代表流程通过。
